# Configuracion de desarrollo para club-manager
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=teamclub
    username: postgres
    password: eduardo

  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:8081/.well-known/jwks.json

clients:
  team-masterdata:
    url: http://localhost:8083
